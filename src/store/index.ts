import { createStore } from 'vuex'
import authentication from './modules/auth'
export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    authentication
  }
})
